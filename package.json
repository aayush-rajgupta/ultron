{
  "name": "ultron-phase1",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/phase1.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "typescript": "^5.8.3"
  }
}
